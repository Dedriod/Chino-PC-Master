/**
 * OBSOLETO en proyectos que usan CodeCertificados.gs v4.5 completo.
 *
 * doGet, doPost, parseRequestBody_, normalizeCodigo_, normalizeSession_ y jsonResponse_
 * están integrados en CodeCertificados.gs. Si pegas ambos archivos en el mismo proyecto
 * de Apps Script, verás error por funciones duplicadas.
 *
 * Mantén solo CodeCertificados.gs en GAS y vuelve a publicar la Web App.
 */
